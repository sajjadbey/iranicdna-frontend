/**
 * Theme System Exports
 * 
 * Central export point for the theme system
 */

export { ThemeProvider, useTheme } from './ThemeContext';
export { ThemeBackground } from './ThemeBackground';
export type { ThemeName, ThemeConfig, ThemeColors, ParticleConfig, ThemeContextValue } from './types';